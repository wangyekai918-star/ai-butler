# AI 管家项目约定

- 后续 AI 管家 UI（包括事件详情五个 Demo）先读取本目录的 [AI管家UI风格规范.md](./AI管家UI风格规范.md)。
- 视觉依据是用户指定的 Figma 文件 Z1fF5tD3nIiUdGaDqLYCLi 中的 3:2、1628:2687、2506:6559 三个节点；具体目标稿和用户最新要求优先。
- 统一使用浅灰底、白色圆角卡、蓝色主色、PingFang SC 中文、Oswald SemiBold 指标数字。具体参数、图表变体和 AI 渐变以规范及 design-reference 内证据为准。
- 参考稿用于统一 UI 风格；业务逻辑、状态和分析顺序按对应 PRD/原型，不根据参考页臆造流程或数据。
- 事件详情复用现有外壳。Logo 为透明底 3 倍 PNG；导航 icon 用原始 SVG；默认选中系统托管。
- 现阶段用户要求静态 UI 交付，顶部功能、侧栏和返回无点击交互。没有新的明确要求时，不增加导航、按钮事件或演示切换。
- 复用现有资源和样式。交付前在浏览器核验布局、资源加载、字体和状态显示，防止仅凭截图猜测尺寸。


- 事件详情每个 Demo 使用独立文件夹，内部包含 同级的 HTML、style.css、app.js，以及 assets/ 和 README.md；运行资源不跨 Demo 引用。保留代码分离，不将 CSS/JS 全部内联进 HTML。历史备份与停用文件归入该 Demo 的 .history/。当前事件详情包含调价策略、促销策略、经营机会、社会事件与通知决策五个 Demo。后续事件详情 Demo 新建同级文件夹，其他业务模块在仓库根目录建立模块文件夹。
- 每个 Demo 的样式统一维护在 `style.css`，用注释区分模块；不要为每个板块再拆出独立 CSS。交互统一维护在 `app.js`，同样用注释区分功能并保留独立作用域；不再按板块新增 JS 文件。HTML、CSS 与 JS 继续分离并在 Demo 根目录同级存放，不额外建立 css/ 或 js/ 文件夹。

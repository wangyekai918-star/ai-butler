# AI 管家 UI 风格规范

版本：1.0 · 核验日期：2026-09-12

## 1. 适用范围与依据

本规范是 AI 管家项目后续 UI 的默认风格依据，包含事件详情的五个 Demo。用户提供的三个 Figma 链接用于参考视觉语言；页面业务信息、分析顺序和状态规则仍依据对应 PRD 与原型。后续有新设计稿或用户明确修订时，以最新要求为准。

已通过 Figma 设计上下文、页面截图和节点属性读取核验，不是仅凭截图估计。

| 参考页面 | Figma 节点 | 主要参考内容 |
| --- | --- | --- |
| 智慧价格 · 总览；原节点名 Frame 1 | [3:2](https://www.figma.com/design/Z1fF5tD3nIiUdGaDqLYCLi/AI管家?node-id=3-2) | 托管概览、价格/收益分析、图表与指标组合、调价计划 |
| 促销经营 · 总览；原节点名 总览 | [1628:2687](https://www.figma.com/design/Z1fF5tD3nIiUdGaDqLYCLi/AI管家?node-id=1628-2687) | 指标网格、促销趋势、活动卡片、选择状态、促销计划 |
| 现场托管公司/列表 | [2506:6559](https://www.figma.com/design/Z1fF5tD3nIiUdGaDqLYCLi/AI管家?node-id=2506-6559) | 汇总指标、表格、实时事件、严重程度标签、环形分布图 |
| 已完成的事件详情基础外壳 | [2689:5015](https://www.figma.com/design/Z1fF5tD3nIiUdGaDqLYCLi/AI管家?node-id=2689-5015) | 顶部栏、侧栏、事件标题和白色内容容器 |

原始读取的样式证据见 [style-evidence.json](./design-reference/style-evidence.json)，整理后的可复用参数见 [style-tokens.json](./design-reference/style-tokens.json)。

## 2. 设计目标

保持轻量、清晰的 B 端数据界面：浅灰背景托起白色卡片，信息靠标题、留白和分组建立层级；主操作和选中态使用蓝色；状态色用于传达业务含义。

渐变主要用于 AI 结论和少量概览内容。不将普通分析卡片都做成渐变底，不引入大面积深色、强投影、发光、玻璃效果或无业务意义的装饰。

## 3. 页面结构

- 三个参考稿均以 1920px 为画布宽度；各稿高度随内容不同，不把参考稿整页高度写死。
- 沿用当前外壳：顶部栏 60px；侧栏 218px；页面外边距 12px；左右主体与纵向主要模块间距 14px。
- 右侧主体随桌面窗口伸缩，字体、图标和侧栏维持原尺寸，避免对整页进行等比缩放。
- 一级模块以白色圆角卡片承载；卡内可以放浅灰指标块、图表、边框卡片和结论区。
- 分析主内容与计划侧栏是参考稿的场景布局，不要求每个事件详情照搬双栏。
- 事件详情的分析步骤保留业务顺序。即使参考总览页有 Tab，也不把存在先后关系的分析步骤改成并列 Tab。
- 分析过程内部的核心指标、异常定位、分析因素各占一整行，按顺序纵向排列；内部标题用小图标，不再重复数字编号，避免与外层四阶段混淆。

## 4. 配色

以下为源节点实测色值。表中“默认用途”是后续新增同类组件的统一选择；具体目标节点有不同参数时按目标节点。

| 用途 | 色值 | 默认用途 |
| --- | --- | --- |
| 页面底色 | #F0F2F5 | 主体背景、分段选择外容器 |
| 卡片底色 | #FFFFFF | 一级内容卡、侧栏、顶部栏 |
| 内嵌指标块 | #F3F5F7 | 小指标卡、计划详情浅灰分组 |
| 主色 | #315EEF | 主按钮、链接、选中标签、激活状态 |
| 主色浅底 | #EEF4FF | 侧栏选中项、选择卡片、蓝色轻标签 |
| 主文字 | #111827 | 重要内容与指标数字 |
| 次级文字 | #344054 | 正文、表格内容、导航文字 |
| 辅助文字 | #667085 | 字段名称、说明和次级信息 |
| 弱文字 | #98A2B3 | 图表坐标等低优先级信息 |
| 弱化状态变体 | #93A1BF | 已完成/失效类弱标签等对应组件 |
| 分隔与描边 | #E6EBF2 | 1px 卡片边框、表格横向分隔、次按钮 |
| 正向/执行中 | #05B37F | 效果、收益或正在执行等有业务依据的状态 |
| 异常/紧急 | #E21818 | 风险指标、紧急标签 |
| 待处理/重要提醒 | #FF6624 | 待审批、重要标签、需注意的信息 |
| 紫色类型标签变体 | #9931EF / #F5EEFF | 特定托管类型；不作为通用主按钮 |
| 时间筛选未选底色 | #F7F8FB | 独立时间胶囊 |

源稿还有局部文字色 #101828、#1F2937、#4B5563、#6B7280，以及 #EF7C31 / #FFF3EE 的橙色类型标签变体。它们保留为具体组件证据，不扩散成更多默认语义色。

上涨/下降的正负号不自动决定红绿。颜色首先表达业务含义；费用上涨、收益上涨、异常数量上涨不能共用同一种判断。事件详情 PRD 对评估指标有专门规则：主指标绿色、其他经营指标中性灰、策略价值指标紫色，制作对应模块时按该业务规则映射。

## 5. 字体与文字层级

| 内容 | 源稿字体 | 常用规格 | 使用方式 |
| --- | --- | --- | --- |
| 中文正文 | PingFang SC Regular | 13–14px | 正文、表格信息、说明 |
| 顶部事件标题、一级页内切换 | PingFang SC Semibold | 16px | 与当前外壳保持一致 |
| 模块标题 | PingFang SC Semibold | 18px；部分主要模块 20px | 同一层级统一尺寸 |
| 子标题/卡片标题 | PingFang SC Semibold | 14–16px | 一眼识别卡片主题 |
| 导航、常规按钮 | PingFang SC Semibold | 13px | 清晰但不抢占内容 |
| 字段名、辅助说明、标签 | PingFang SC Regular / Semibold | 12px，常见行高 16/18px | 状态和标题用 Semibold，说明用 Regular |
| 汇总 KPI 大数字 | Oswald SemiBold，字重 600 | 36px | 顶层关键数据 |
| 普通指标数字 | Oswald SemiBold，字重 600 | 24px | 内嵌指标块 |
| 次级数字 | Oswald SemiBold，字重 600 | 14/18px | 次级统计、比率等 |
| 单位 | PingFang SC；个别数字单位用 Oswald | 多为 12–14px | 与数字分开设置，靠基线对齐 |
| 图表坐标/英文数字 | 部分图表使用 Inter | 按图表实际节点 | 保持低优先级、清楚可读 |

重要约定：

- 指标数字已核实为 **Oswald SemiBold**，后续不能凭观感替换成 DIN、Arial 或中文字体。
- 指标项内数字、中文标签、单位和日期范围的行高统一为自身字号的 100%（CSS `line-height: 1`）。期间卡片的标题靠左上，日期范围靠左下，不用额外行高撑开文字。
- 事件分析的核心指标卡片按内容自然撑开，当前视觉高度约 74px，不设置 88px 最小高度；内边距与标题至指标数值的间距均为 12px，避免中间留白过大。
- 已接入用户提供的 Oswald SemiBold 字体：`事件详情/经营风险_调价策略/assets/fonts/Oswald-SemiBold.woff2`。公共 `shell.css` 已声明字重 600 的 `@font-face` 和 `--font-number`；指标数字使用 `.metric-number`，字号按对应层级设置。源文件来自豪能汇大屏的同名字体，仅复制到本项目，不依赖其他项目的相对路径。
- 不给所有正文统一加粗。标题和数字突出，说明、字段名称保持正常字重。
- 日期范围统一用中文「至」连接，例如「08-11 至 08-17」；不使用横线连接起止日期。单个日期内部的年月日分隔格式保持原有口径。
- 源稿多数中文行高是 AUTO；新内容的正文行高可按约 1.5 倍作为落地默认值，具体稿有明确值时保留原值。
- 部分图表存在缩放后的小数字号与小数描边；它们是局部图表证据，不升级为整个项目的字号规则。

字体证据示例：36px 数字 3:129、2506:6588；24px 数字 4:159、1650:4077；18px 标题 1:133、1650:4027；20px 标题 2506:7113。

## 6. 卡片、间距与圆角

| 组件层级 | 圆角 | 内边距/间距 | 外观 |
| --- | --- | --- | --- |
| 一级模块/外层内容容器 | 16px | 常见内边距 16px，内部同级模块间距 8px | 白底，通常无额外重投影 |
| 事件标题条 | 12px | 内边距 14px，高度 50px | 白底 |
| 内嵌指标块 | 10px；部分指标组件 12px | 内边距 12px，字段与数字间距 8px | #F3F5F7 |
| 策略/活动卡片 | 12px | 常见内边距 12px，内部间距 12px | 白底、1px #E6EBF2 |
| 主/次按钮 | 8px | 常规高度 34px，左右 12–16px | 主按钮纯蓝、次按钮白底细边 |
| 状态标签 | 4px | 高 22px，左右 5–7px | 轻底色或同色浅描边 |
| 时间筛选胶囊 | 全圆角；源值 52px | 高 28px / 30px，左右 13px | 选中浅蓝底蓝字 |
| 图标与文字 | — | 常见间距 4/6/8px | 同行垂直居中 |
| 内部小分组 | — | 常用 4/8/12px | 不额外叠套无意义的大卡片 |

14px 是外层模块间距，不强行改成 16px；源稿中 7px 的操作组间距等局部值按同款组件保留，不要求所有值都归为 8 的倍数。

用户最新修订（2026-09-12）：核心指标卡、异常定位子卡等内部同级模块之间的横向和纵向间距统一为 8px，使用 `--inner-module-gap`；卡片内部的 12px 内边距与标题到数值间距按组件自身规则保留。

卡片通过背景、轻描边和间距区分。局部选中块有极轻阴影：0 2px 6px #000000 5%；不将该阴影批量加到所有白卡上。

用户明确修订：卡片标题下方不加分割线，使用间距区分标题与内容；事件描述应归入基本信息容器，不作为上下两个容器之间的独立段落。

事件信息按原型的网格布局展示：字段名在上、字段值在下，8 个字段以两行四列组织，第一列略宽，单元格之间用轻描边区分。事件描述是网格底部的整行内容，不加背景填充，不将所有标签与值挤成同行排版。

证据示例：一级卡 1:130、1:189、2506:7111；指标块 1:250、1650:5772、2506:6834；计划卡 1:374、1650:5296。

## 7. 关键组件

### 按钮与选中状态

- 主按钮：#315EEF 填充、白字、13px Semibold、34px 高、8px 圆角。
- 次按钮：白底、1px #E6EBF2、深灰字，与同组主按钮等高。
- 单项选中：#EEF4FF 浅底 + #315EEF 文字；需要区分整张可选择卡时加 1px 主色边框。
- 分段选择：#F0F2F5 外容器，内边距 3px、圆角 10px；选中项白底蓝字、圆角 8px。参考实例整体高 37px，内项高 31px。
- 时间筛选使用轻量胶囊，和模块级下划线 Tab、分段选择分别保留其用途，不混成同一种控件。

### 标签

- 填充型标签：文字使用语义色，背景为该色 10% 或明确的浅色 token。
- 描边型执行状态：22px 高，1px 同色 30% 描边，保持文字足够清楚。
- 蓝色提示通常使用 #EEF4FF。
- 严重程度使用颜色加明确文字；不能只靠颜色区分紧急、重要、提醒。
- 具体状态名称仍取业务定义，参考稿的“待审批/待执行”等不能擅自覆盖事件详情 PRD 的状态口径。
- 「AI 修正」使用用户最新提供的原始 SVG：`事件详情/经营风险_调价策略/assets/AI修正.svg`，按 54 × 18px 显示。提示符号已集成在 SVG 内，外部不再添加问号或信息图标，也不额外叠加底色或描边。整张标签支持悬停或键盘聚焦查看修正说明；没有修正时标签和提示一并隐藏。

### 指标卡

常见结构是字段名 → 指标数字及小单位 → 变化或状态说明。大数字与单位分层，多个指标沿同一基线、统一小数位规则，增减方向和对比口径写清楚。

顶层关键指标用白卡 16px 圆角；图表旁的补充指标用浅灰小块。智慧价格概览中的少量特殊 KPI 使用从左到右 #F4F7FF → #F8F4FF 的淡渐变，源实例圆角 8px；不推广到全部指标卡。

### 表格与事件列表

- 表格容器：12px 圆角，1px #E6EBF2。
- 表头：#315EEF 6% 底色，12px Semibold。
- 数据文字：13px Regular，#344054；异常数据按语义色显示。
- 源稿表头实测 48.5px，普通行 61px；这是该列表的参数。新表格没有具体稿时可取约 48px / 60px 的默认高度，并按实际内容留出空间。
- 单元格左右内边距 16px；以横向细分隔为主，避免粗重全网格。
- 实时事件行采用缩略图 + 标题/等级 + 时间等辅助信息 + 右侧入口，缩略图仅作为必要内容证据。
- 缩略图、图表、卡片在不同宽度下不挤压文字到不可读。
- 充电时段等内部滚动列表通过露出半行内容与底部白色渐变遮罩提示后续内容，不在数量旁添加「可滚动」说明。滚到底或内容不足以滚动时隐藏遮罩，避免遮挡末行；表头固定，遮罩不拦截操作。

## 8. 图表风格

- 浅色坐标轴和细网格，留足图内空白。
- 柱形以淡蓝为底；蓝/绿/橙/紫曲线用来区分数据系列，采用小圆点和清楚的图例。
- 实线、虚线有区分，但具体哪条代表实际/预测/基准由业务定义决定，不从风格参考推断。
- 常见曲线约 3px；促销参考图表因缩放实测为 3.17–3.29px。不要把所有折线改为细到难以辨认的 1px。
- 图例多在图下居中，圆点小而清楚；右侧可以搭配 2×2 或多列指标块。
- 分布图采用简洁环形图，中心数字，旁边列类别、数量、占比；不增加 3D、发光或厚阴影。
- 日期、单位、双轴含义、累计/当次口径以对应 PRD 为准；原型缺少的数据不能凭视觉需要补造。

| 已核实图表变体 | 柱/基底 | 曲线 | 网格 |
| --- | --- | --- | --- |
| 价格分析 1:200 | #DBE7FF | 蓝 #3B63E6；橙 #FFA400 标记 | #EDF0F5，约 1px |
| 收益分析 4:324 | #DBE7FF | 绿 #05B37F | #EDF0F5，约 1px |
| 促销预算 1650:4106 | #DFE8FF | 蓝 #3D63F2；橙 #F59E0B 虚线 | #EEF2F7；轴 #DBE4F2 |
| 用户运营 1650:4262 | #DFE8FF | 绿 #16A56B；橙 #F59E0B 虚线 | #EEF2F7；轴 #DBE4F2 |

源稿还使用 #7C3AED 作为促销图例的紫色系列。参考图表之间存在细微配色差异，使用对应变体，不在同一张图中随意混用。

## 9. AI 结论/洞察区域

这是三个页面共同的识别元素，需稳定复用：

- 圆角 12px，边框 1px。
- 三段渐变色标：0% #FF9124 → 53.365% #FF1080 → 100% #42C4FF。
- **背景渐变每个色标均为 8% 透明度；边框渐变为 100% 不透明度。**
- “AI判断/AI洞察/AI复盘提示”等前缀用 14px Semibold，结论正文用 14px Regular、#111827。
- 常见内边距：上下 12px、左右 14px；窄侧卡是四边 12px。
- 三个实测实例按自身宽高换算的 CSS 角度分别约 100.17°、92.09°、99.52°，视觉为左暖右冷、轻微倾斜。完整 gradientTransform 保存在证据文件中，不能把整块 opacity 设成 8%，否则文字也会变淡。
- Figma 自动生成代码把渐变描边简化成了橙色边框；直接节点属性已确认应为三色渐变描边。后续实现保留真实描边。
- AI 结论紧邻其解释的图表/指标，先给判断再给简短依据。不要让渐变面积大过主要数据内容。

源节点：1:278、1650:5808、2506:6843。

## 10. 素材与落地要求

- 当前 Logo 使用 Figma 页面内实例导出的透明底 3 倍 PNG；实际尺寸 399×108px，显示尺寸 133×36px。
- 导航图标为 SVG，18px；其他控件图标按对应节点的 16/22/24px 尺寸。
- 优先复用已导出的原始素材，不重绘近似图标，不用 emoji 临时替代。
- AI/业务插画仅在参考组件本身需要时使用，体积和视觉强度不能压过数据。
- 正式 HTML 引用本地资源，不能依赖会过期的 Figma 临时素材 URL。
- 当前外壳中「系统托管」默认选中；顶部功能、侧栏和返回均为静态展示。风格参考中的可操作按钮不构成本项目新增交互的授权。
- 后续填充内容时显式设置正文 Regular 与标题 Semibold，避免继承现有外壳的全局 600 字重。
- 先复用事件详情的 shell.css 和 assets，再按这份规范补充模块样式；不因参考页切换而改变外壳样式。

## 11. 对五个事件详情 Demo 的应用

| Demo | 优先参考的视觉模块 |
| --- | --- |
| 经营风险 · 调价策略 | 价格/收益分析、指标块、调价计划卡、AI 结论 |
| 经营风险 · 促销策略 | 促销指标网格、活动卡、趋势图、执行状态 |
| 经营机会 · 五步机会评估 | 共用指标卡、状态标签与 AI 结论；保持五步顺序 |
| 社会事件 · 权限站点筛选 | 站点表格/列表、轻选择状态、共用卡片层级 |
| 经营风险 · 通知决策 | 共用事件信息、状态与计划卡；通知发送结果在策略模块闭环 |

事件详情按用户当前制作阶段逐块补充业务内容，完成范围以事件详情目录的 README 为准。

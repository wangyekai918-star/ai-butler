# 经营风险 · 调价策略

[在线演示](https://wangyekai918-star.github.io/ai-butler/%E4%BA%8B%E4%BB%B6%E8%AF%A6%E6%83%85/%E7%BB%8F%E8%90%A5%E9%A3%8E%E9%99%A9_%E8%B0%83%E4%BB%B7%E7%AD%96%E7%95%A5/%E7%BB%8F%E8%90%A5%E9%A3%8E%E9%99%A9_%E8%B0%83%E4%BB%B7%E7%AD%96%E7%95%A5.html) · [返回 Demo 目录](../README.md)
本页现仅加载一个 `style.css`。合并遵循原 7 份 CSS 的加载顺序，保留模块注释及覆盖关系；HTML、JS 和素材仍保持分离。合并前的 HTML、README 及原 CSS 位于 `.history/2026-09-12-before-css-merge-181316`。后文涉及旧样式文件的历史记录仅说明当时改动，当前修改统一在 style.css 中完成。

交互代码现统一为 `app.js`，按原顺序保留四个独立功能区：核心指标、异常定位、决策复盘、生命周期导航。原 4 份 JS 和引用修改前的 HTML 已归入 `.history/2026-09-12-before-js-merge-181606`；后文出现的旧 JS 文件名仅为历史改动记录。当前交付保留一份 HTML、一份 CSS、一份 JS，以及 assets 和 README。

style.css 和 app.js 与 HTML 同级，不再单独建立 css、js 文件夹。目录调整前的文件保存在 `.history/2026-09-12-before-flat-demo-files-181942`。

此文件夹是可独立复制的 Demo。HTML、样式、脚本和素材均在本目录内，运行不依赖其他 Demo。

```text
经营风险_调价策略/
├── 经营风险_调价策略.html
├── style.css
├── app.js
├── assets/
├── README.md
└── .history/
```

交付时复制 HTML、style.css、app.js、assets 和 README.md 即可；`.history` 保存改动备份及停用文件。此次目录整理前完整快照位于 `.history/2026-09-12-before-demo-folders-180851/`。历史快照保留原目录结构，回退时应整套使用，不能与当前文件路径混用。

后续页面统一遵循 [AI 管家 UI 风格规范](../../AI管家UI风格规范.md)，参数和 Figma 节点证据位于上两级的 `design-reference` 目录。

当前页面：`经营风险_调价策略.html`，已完成基础外壳、二级返回标题、「事件信息」、「分析过程 → 核心指标、异常定位、分析因素、建议动作」、「决策生成」、「执行复盘」和右侧四阶段生命周期。建议动作与前三个模块同级，是分析过程最后一个模块。

事件信息与分析过程两张外卡片统一使用 12px 内容内边距。分析过程为纯白卡片，无外描边或整卡渐变；核心指标、异常定位、分析因素、建议动作持续展示，用各自的淡蓝至白色横向渐变标题横幅分隔。四条横幅直接延伸到外卡片左右边缘，正文统一距外卡片左右边缘 12px；父标题和最底部内容保留 12px 内边距，模块间距 8px。异常定位按标题、完整结论、数据表格排列，综合判断在底部通栏展示。本页样式统一位于 `style.css`，内部按模块保留注释分区。此版尚未更新为项目通用规范。

旧版整卡渐变已停用。历史 Figma 参考参数保留在 `../../design-reference/analysis-gradient-reference.json`，旧渐变方案修改前版本位于 `.history/2026-09-12-before-analysis-gradient-114706/`；不作为当前页面样式。

最初结构的完整备份位于 `.history/2026-09-12-before-analysis-cards-114029/`，包含 HTML、全部样式、脚本和素材。回退任一版本时，请从对应 `.history` 备份恢复配套 HTML、CSS 和脚本。

直接用浏览器打开 HTML 即可，不需要安装依赖。复制交付时保留 HTML、`style.css`、`app.js`、`assets/` 和 README.md 即可。旧 CSS 已归入历史备份；停用的折叠样式与脚本位于 `.history/unused-collapse/`，均不参与页面加载。

- 设计来源：https://www.figma.com/design/Z1fF5tD3nIiUdGaDqLYCLi/AI管家?node-id=2689-5015
- 基准画布：1920 × 1080；顶部栏 60px，左侧栏 218px，页面边距 12px，模块间距 14px。
- 桌面布局：侧栏固定宽度，右侧区域随窗口伸缩；最小页面宽度 1024px，小于此宽度时横向滚动。
- Logo：Figma 原节点导出的 3 倍 PNG，399 × 108px，显示为 133 × 36px。
- 导航及返回图标：Figma 原始 SVG，本地引用。
- 数字字体：已接入 `assets/fonts/Oswald-SemiBold.woff2`，字重 600；由用户提供的字体原文件复制，数字及常用标点覆盖已核验。后续指标数字使用 `.metric-number` 类，字号按指标层级设置，中文和单位独立排版。
- 顶部功能、左侧栏、返回及四个分析模块标题均为静态展示。AI 修正标签支持悬停说明，趋势图支持数据提示，充电时段支持内部滚动；分析模块无折叠功能。右侧生命周期支持页内定位与滚动高亮；没有接口请求或业务提交操作。
- `style.css` 第 01 节保存基础变量、字体与外壳，第 02 至 07 节分别组织事件信息、生命周期、分析模块和决策复盘。
- 统一样式中的事件卡片与生命周期分区控制整体布局；工作区采用 218px 左侧栏 + 自适应主内容 + 220px 生命周期的三列布局，生命周期与左侧栏顶部、底部对齐。事件信息参考原型：字段名在上、字段值在下，单元格以细线区分；下方通栏 AI分析结论与字段列数适配见下方「事件信息布局修订」。小于 1100px 时生命周期缩为 200px。
- 后续业务卡片添加到 `.event-sections`，右侧生命周期展示完整阶段。

## 生命周期导航（2026-09-12）

- 右侧导航提升为工作区第三列，与左侧栏同级；两侧均为 sticky，顶部距视口 72px，高度为视口减去顶部栏 60px 及上下各 12px，最小高度 588px，白底、16px 圆角。返回标题仅占中间内容列。主内容实际宽度保持原样。
- 移除四个阶段旁的“已完成 / 进行中”。保留阶段名称和时间，默认中性灰；当前位置采用 #315EEF 蓝色数字圆标、标题和 #EEF4FF 浅蓝底，执行复盘不再使用绿色。
- 四项为原生锚点链接，分别对应 `event-information`、`event-analysis`、`decision-section`、`execution-review`。支持整行点击、键盘 Enter、可见焦点、URL 锚点和浏览器返回；点击后平滑定位到顶部栏下方；高亮仅作用于右侧导航，中间卡片标题及编号保持原样。减少动态效果偏好下直接定位。
- `app.js` 的第 04 节根据页面滚动更新唯一高亮，内部表格滚动不切换阶段；两个并排卡片共享纵向位置时保留用户点击的阶段，正常向下进入这一行默认选中决策生成。动画期间保持目标高亮，抵达页尾时也能正确识别最后一行。
- 已核验四项点击、键盘跳转、手动 PageUp 滚动联动及 1024 / 1280 / 1920px 布局。两侧导航在 720 / 900 / 1080px 高度下分别等高 636 / 816 / 996px；无状态文字、绿色选中或新增横向溢出。
- 修改前备份：`.history/2026-09-12-before-lifecycle-nav-175433/`。

## 决策生成与执行复盘（2026-09-12）

- 新增 `css/decision-review.css`、`js/decision-review.js`，数据集中在 HTML 的 `decision-review-data` JSON 中，无接口请求或业务提交。改动前备份为 `.history/2026-09-12-before-decision-review-173405/`。
- 两块位于分析过程下方：左侧决策生成、右侧执行复盘；外卡纯白、16px 圆角、12px 内边距，两卡间距 14px（横排及竖排一致）。主内容宽度至少 714px 时并排，决策较窄、复盘较宽；不足时自动上下排列。标题不加分割线和折叠。
- 决策展示：策略状态、调价项目、分时调整列表、生效时间、执行方式、影响范围、同步动作、调价目的。服务费调整列表由 `decision.slots` 提供，支持多时段和单条全天统一价。`isAllDay: true` 或 `00:00` 至 `24:00` 会显示“全天统一价”；旧版未传 slots 时可用 `adjustAmount` 回退为全天调整；明确空数组显示“暂无调价时段”。
- 调整幅度与异常定位表格统一使用 18px Oswald SemiBold，12px 常规字体单位、基线对齐、3px 间距、100% 行高；保留 4 位小数和正负号，负红正绿。当前 8 条示例中，00:00 至 08:00、13:00 至 16:00、22:00 至 24:00 为正向调整，其余 5 条为负向调整。日期和时段范围统一用“至”。
- 长列表的表头固定，通过半行露出与底部白色渐变提示后续内容。各窗口宽度统一显示 3.5 行（表头 32px + 3.5 × 40px = 172px），滚到底后移除遮罩；只有一条时自然收缩到 72px（表头加一行）。没有可见的“可滚动”文案。
- **数据来源和边界：**业务展示规则依据 PRD 第 5.1、6.1、6.2 节；执行指标、趋势、默认评级及结论沿用产品原型。8 个调价时段为本次多时段 UI 的演示扩展，其中正向调整按用户要求补充，用于展示涨降价并存的视觉效果；负向幅度沿用原型建议范围；执行结果没有按这组新时段重新计算。正式接入时需整体替换为同一策略的真实数据。
- 决策信息最新排版：所有字段纳入同一宫格，白底、1px #E6EBF2 细线、10px 圆角；生效时间整行，执行方式与影响范围并排，同步动作整行，调价目的在最后整行。各格内边距 10px 12px，名称在上、值在下、间距 4px；名称 13px 灰色，值 14px Semibold，目的正文保持 Regular。日期窄屏换行时“至”与结束时间一起换行。调整前备份 `.history/2026-09-12-before-decision-grid-174842/`。
- 已移除复盘评级右侧额外的“评估期间”起止日期，保留原型阶段评估与结果内容。
- 执行默认 `EVALUATING`：评估中、阶段评估“优”、阶段结论。`PENDING_REVIEW` 显示待复盘和最后一次阶段评估；`REVIEWED` 显示已复盘、复盘结果和复盘结论。优/良/差均有 PRD 对应的默认文案，业务传入的结论优先。
- 回调由 `callbackTriggered` 明确提供，前端不会根据一次评级“差”自行触发。未回调的“差”仅显示重点观察说明；已回调说明原因，待复盘时追加等待最终结果。未生成复盘时不显示最终成功结论。
- 四项指标同时展示值和变化幅度：充电量、充电用户、服务费收入、调价收益。前三项变化率口径为“较策略评估基准”，调价收益沿原型“较不调价基准”。充电量绿色、其他经营指标中性灰、策略价值紫色；收入 -0.7% 不自动标红。缺失或非有限数值显示“—”，不当作 0。
- 图表采用原生 SVG 四条线，并明确标注“累计效果指数”：原型各系列首点为 100，不能当作度/元/人展示，也不能据此反推上方 KPI。节点对应从策略开始到当前评估时点的累计结果；数据源按 PRD 提供不足 3 日的阶段节点或 3 日及以上的日节点，前端不补点或伪造累计值。日期含时间时使用两行坐标。
- 复盘指标在右卡宽度至少 720px 时排成四列，否则为 2×2。图表按容器尺寸重绘，支持悬停、键盘聚焦提示与 Esc 关闭。正式页面不增加状态切换开关。
- 已核验 1920px、1280px 并排和 1024px 单列，卡片内边距 12px，文字无横向溢出；8 个调价时段、24 个趋势点、全部指标与变化率正常显示；已验证单时段自然收缩、滚到底隐藏遮罩、缺失数据、待复盘、已复盘和已回调状态。临时核验页面仅保留在 `.history/decision-review-qa/`，不参与正式页面加载。

## 分析过程与标题横幅（2026-09-12）

- “分析过程”父标题栏在该卡片范围内吸顶，固定在顶部导航下方并保留 12px 间距（top: 72px），与左右导航顶部对齐，保留白底、12px 内边距和原有深色标题，不增加高亮或分割线。浏览核心指标、异常定位、分析因素及建议动作时始终保留阶段名称；离开分析过程后自然滚出，不跟随到决策生成或执行复盘。使用原生 CSS sticky，不复制标题，也不增加滚动脚本。吸顶栏采用完整页面灰底遮挡层与白色圆角背景两层绘制，遮住上方 12px 留白及圆角外侧，避免底层滚动内容穿透；遮挡层不拦截操作。

- 分析过程整体为 #FFFFFF，无外描边或纵向背景渐变；父标题保持 12px 内边距，分析子模块不再叠加外层左右内边距。正文距外卡片左右各 12px，末个模块底部留白 12px，内部指标卡自身的内边距保持原样。
- 四个模块的标题横幅使用 `linear-gradient(90deg, #EEF4FF 0%, #FFFFFF 100%)`，从左侧淡蓝过渡到右侧纯白。横幅以左右 -12px 外边距穿过模块内边距，直接通栏，与外卡片两侧齐平；横幅高 44px、无圆角，标题区域左右内边距 12px、上下各 10px。标题保持左对齐并垂直居中，图标与标签同行居中；不添加标题分割线。
- 标题恢复普通 h3，移除折叠按钮、文字、箭头和事件绑定，四个模块始终展示完整内容。旧折叠 CSS 与 JS 已解除引用。
- 「AI 修正」仍使用 54 × 18px SVG，紧邻核心指标文字右侧、间距 8px；悬停和聚焦说明保留。「推荐进入调价决策」仍为建议动作标题右侧的静态标签、间距 8px。
- 修改前备份：`.history/2026-09-12-before-analysis-banners-171756/`，包含 HTML、布局样式、旧折叠样式与脚本和 README。
- 浏览器核验：1024、1280、1920px 下四条横幅均高 44px，标题上下留白均为 10px，无页面横向溢出；分析过程背景为 #FFFFFF、背景图像 none、边框 0px。页面无折叠按钮及折叠资源引用，五个内容节点持续可见；趋势图保留 14 个数据点，AI 修正说明正常显示，异常定位两列等高，控制台无错误或警告。
- 通栏修订核验：1280px 下四条横幅距外卡片左右边缘均为 0px，四个模块内容距外卡片左右边缘均为 16px，最后一个模块底部留白 16px，标题上下各 10px。实际分析容器宽 740px 时核心指标两列为 320px / 380px；宽 679px 时核心指标及异常定位均恢复单列，无横向溢出。修改前备份：`.history/2026-09-12-before-fullwidth-banners-172204/`。

## 事件信息（2026-09-12）

- 原型来源：用户提供的 `经营风险_调价策略.html`。本页采用实际运行的 `mock.event` 和 `mock.lifecycle`，保留下载目录中的原型文件。
- 字段：事件名称、事件编号、事件类型、发现时间、影响时间范围、影响范围、当前状态、异常指标；另含事件描述、分析结论。
- 数据口径：08-11 至 08-17 累计充电量 9,834.000 度；较 AI 修正后的 07-28 至 08-03 对比周期减少 845.000 度，降幅 7.91%。原型静态占位中的单日数据被运行时覆写，因此本页不采用旧占位值。
- 日期范围统一使用中文「至」连接，例如 `08-11 至 08-17`、`2026-08-11 至 2026-08-17`。
- 生命周期：事件信息 → 分析过程 → 决策生成 → 执行复盘。前两项按用户要求与左侧模块名称一致，第三阶段名称已由用户确认。前三阶段完成，执行复盘进行中，与原型「策略执行中」状态一致。
- 右侧生命周期按用户最新要求恢复白色圆角卡片：背景 #FFFFFF、圆角 16px、内边距 16px；小于 1100px 时左右内边距为 12px。标题保留「事件生命周期」，使用 16px / 600 字重 / 28px 行高，与下方编号时间线左对齐；标题行与左侧「事件信息」标题行中心对齐，距离时间线 16px。保留新阶段名称、滚动吸顶行为及 220px / 200px 侧栏宽度，标题下不加分割线。
- 分析结论按原型与 PRD 3.2 展示：分析已完成时显示，未完成时应隐藏。PRD 末尾另有基本信息不混入结论的表述，本版按新原型的明确字段处理。
- 事件描述位于基本信息网格底部，横跨全部列，不加背景填充，以「事件描述：」前缀区分；分析结论单独放在信息网格右侧，约占 30% 宽度，与左侧等高；窄屏恢复下方通栏。结论区使用 8% 三色渐变底、100% 三色渐变描边。金额/电量/比例类指标数字使用 Oswald，日期和事件编号沿用正文体系。
- 卡片标题下方不加分割线，事件信息与生命周期均采用间距区分标题和内容。
- 当前为本地 UI 参考，没有接口请求或未制作模块的占位内容。
- 浏览器核验：1920、1440、1280、1024px 均无字段溢出，左右栏间距 14px；Logo、SVG 与数字字体正常加载，正文为 400 字重，页面无控制台错误。

## 事件信息布局修订

事件信息使用通栏字段宫格，事件描述保留在宫格底部；AI分析结论位于宫格下方，独占一整行，与宫格间距 8px。标题统一为「AI分析结论」，沿用 AI 渐变样式。内容宽度达到 720px 时字段为两行四列，窄窗口为两列；分析结论始终放在下方。

本轮核验：1920px 下左侧 973px、右侧 417px，均高 204px；1280px 下左侧 525px、右侧 225px；1024px 下恢复上下排列。字段、日期和结论无溢出，事件描述仍无背景填充，页面控制台无错误。修改前版本位于 `.history/2026-09-12-before-event-summary-side-152354/`。在线演示地址为 `https://wangyekai918-star.github.io/ai-butler/%E4%BA%8B%E4%BB%B6%E8%AF%A6%E6%83%85/%E7%BB%8F%E8%90%A5%E9%A3%8E%E9%99%A9_%E8%B0%83%E4%BB%B7%E7%AD%96%E7%95%A5/%E7%BB%8F%E8%90%A5%E9%A3%8E%E9%99%A9_%E8%B0%83%E4%BB%B7%E7%AD%96%E7%95%A5.html`。

按用户最新要求采用原型的上下字段布局，避免将所有标签和值挤在同一行。单元格内边距 10px 13px，字段名 13px / #667085，字段值 14px / 600 字重，标签和值间距 4px。事件描述为 14px / 400 字重，与基本信息共用容器。生命周期时间为 13px / #667085；绿色小字使用 #07865F 提高对比度。

本次改动前的同行排版样式保存在 `.history/2026-09-12-before-prototype-info-layout`。

## 分析过程 · 核心指标

- 当前启用横向布局预览：核心指标标题占满一行，下方左侧为两列三行指标，右侧为趋势图。三行依次为「分析期间 / 对比期间」「分析期间充电量 / 对比期间充电量」「变化值 / 变化幅度」，两侧上下对齐，指标卡保持约 74px 自然高度、8px 间距。左右按约 2:3 分配，最小宽度分别为 320px、380px；分析容器不足 732px 时恢复上下结构（320 + 380 + 8 + 左右内边距 24），避免日期与坐标拥挤。图表根据实际容器宽高绘制，文字和线宽不随图形缩放。样式位于 `style.css` 的第 05 节，本次修改前版本保存在 `.history/2026-09-12-before-core-horizontal-120135/`。
- 横向预览已在当前 1280px 浏览器中核验：核心指标模块高度由 515.5px 降为 299px，左侧指标宽 320px、右侧图表宽 422px，高度均为 236.5px；6 张指标卡各 73.5px，无日期、数值或图例溢出。图表提示可正常显示及通过 Esc 关闭，控制台无错误；父容器标题上下 16px、1px 白框和子卡外沿 8px 均保持。
- 外层保留「02 分析过程」，内部使用 SVG 小图标，不重复 1、2、3 编号。核心指标、异常定位、分析因素采用纵向整行排列。
- 6 项指标：分析期间、分析期间充电量、对比期间、对比期间充电量、变化值、变化幅度。基础样式保留原先大屏一行六列、较窄桌面两行三列的配置；当前横向预览在空间足够时覆盖为两列三行。
- 指标卡之间的横向和纵向间距均为 8px，与异常定位子卡统一使用 `--inner-module-gap`。
- 指标卡内的数字、中文标签、单位与日期范围统一使用 `line-height: 1`（自身字号的 100%）。按用户图 2 的紧凑高度，取消 88px 最小高度限制，由内容撑开，当前字体下约 74px；内边距 12px，标题与指标数值间距 12px。标题靠左上，两个期间的日期范围靠左下并距底部 12px，数字和单位按基线对齐。
- 趋势图使用原型的 7 对样本。横轴只显示分析日期，蓝色实线为实际充电量，橙色虚线为对比期间基线充电量；图例在下方。悬停或键盘聚焦数据点展示实际值、基线值及对应对比日期，Esc 收起。
- 「AI 修正」原样引用用户最新提供的 `assets/AI修正.svg`（54 × 18px），提示符号已集成，外部不再添加问号或信息图标；整张标签悬停或键盘聚焦显示「基线修正说明」，Esc 收起。只有 `baselineCorrection.corrected === true` 时显示整个标签与提示，false 或缺失时全部隐藏，不保留占位。
- 修正说明取自原型：08-04 至 08-10 受大型活动影响不可比，回溯一个完整周期，最终采用 07-28 至 08-03。
- 数据统一放在 HTML 的 `core-analysis-data` JSON 中，`app.js` 的第 01 节渲染指标和 SVG 趋势图，ResizeObserver 保持图表尺寸随容器变化。没有图表库或外部 CDN 依赖。
- PRD 有「近四个可比周均值」的命名设想，但本次原型数据明确是一个修正后的对比周期，因此使用「基线充电量（对比期间）」。后续改为四周均值时，需同步实际数据、指标期间、图例和提示说明。
- 已核验 1920、1600、1440、1280、1024px 下的布局及资源；核心指标始终占满内容宽度，标题无分割线，6 项指标无溢出，图表有 14 个数据点。AI 说明与趋势提示可以显示并通过 Esc 收起。

## 分析过程 · 异常定位（2026-09-12）

- 最新布局为两列：左侧用户结构分析、充电入口分析上下排列，右侧充电时段分析。三个维度均按「标题 → 完整结论 → 数据表格」排列，先读结论、再看数据。底部另设通栏「综合判断」，仅展示原有 crossConclusion。
- 每张分析卡只保留一层白色外卡（1px #E6EBF2 描边、12px 圆角），表格横向铺满，无独立内框或内层圆角。标题顶部内边距 10px、左右 12px，标题下不额外加线；无结论时标题底部内边距为 10px。表头 32px、数据行最小 40px。
- 结论描述直接放在卡片标题下方 4px 处，移除「定位结论：」前缀，与表格间距 12px；左右与标题容器及单元格对齐，使用较浅的 #667085，正文 14px Regular / 22px 行高。没有背景色、额外容器或自动推底留白。
- 综合判断保留 AI 三色渐变与 1px 描边，以 14px 半粗体标题和 14px 正文左右排列，自然换行，整条横跨两列。与上方分析卡间距为 8px，只有整体结论缺失时隐藏此条。
- 分析容器宽度至少 672px 时采用两列（300 + 340 + 8 + 左右内边距 24），宽度比约 35:40，最小宽度分别 300px、340px；不足时恢复单列。左侧两卡按内容自然撑开，右侧长列表与左列总高度对齐，不拉伸左侧表格行高。
- 时段列表从可用高度中扣除标题和定位结论，再计算完整行加半行的显示量；数据行不低于 40px，默认 8 条仍可内部滚动。保留 24px 底部渐变，滚到底后隐藏；表头固定，时段结论始终在滚动区域外，标题只显示时段数量。
- 单列视口 172px，显示 3.5 行。只有「全天统一价」或不超过 3 条时列表自然收缩；单条列表高 72px，无滚动遮罩。无时段时显示空态并隐藏时段结论。缺少用户或入口结论时仅隐藏该卡结论，不影响其他维度和综合判断。
- 数据仍使用原有 user.conclusion、channel.conclusion、timeConclusion 与 crossConclusion，未修改结论内容或推断新结果。PRD 4.1 明确允许两类用户、两个入口及 TOP1 与 TOP2 两个时段同时聚焦，完整自然语言结论可容纳这些情况，不引入 crossFocus 或单一结果限制。
- anomaly-analysis.js 渲染数据、同步列表高度与遮罩；ResizeObserver 监听左列、网格、时段标题和结论，随字体、宽度或文本换行重新计算。方向键按实际行高滚动，列表聚焦后的翻页与首尾键保持原有行为。
- 数字使用 Oswald SemiBold，数字与单位行高 100%；下降红色、上升绿色、占比中性色。时间范围用中文「至」。主渠道名称沿用原型的自营与加盟站规则。
- 用户与渠道数据保留原型示例，与核心总变化值尚未完全对账，仅作 UI 参考。8 时段是原型三个大时段的细分示例，总变化值 -845.000 度、占比合计 100%，不代表新增真实业务数据。
- 上一版浏览器核验：1920px 下左右两列均高 400px，综合判断横跨 1390px 内容宽、高 48px；时段显示 6.5 行。列表滚到底后渐变隐藏，无横向溢出。
- 修改前完整备份：.history/2026-09-12-before-anomaly-local-conclusions-163612/，包含 HTML、布局 CSS、异常定位脚本与 README。
- 结论前置调整前备份：.history/2026-09-12-before-anomaly-conclusion-first-164629/，包含 HTML、布局 CSS 与 README。
- 结论前置后浏览器核验：1280px 下三卡均为标题、结论、表格顺序，标题与结论间距 8px、结论底部留白 12px，无「定位结论」前缀。左右两列均高 424px，综合判断横跨 750px；时段可内部滚动，滚到底后遮罩隐藏，无横向溢出及控制台警告或错误。

## 分析过程 · 分析因素与建议动作（2026-09-12）

- 页面结构依次为「核心指标 → 异常定位 → 分析因素 → 建议动作」。四者都是 `.analysis-sections` 的直接子模块，使用同一标题规格、小 SVG 图标和 8px 间距；建议动作不嵌套在分析因素内，也不属于后续决策生成模块。
- 层级修订：主因素、次因素、参考因素、排除因素是 `.factor-groups` 下四个同级分组，统一纯白底、1px #E6EBF2 描边、12px 圆角及 12px 内边距。参考因素恢复外框，标题取消单独左侧偏移，与同列主因素、排除因素左对齐，内部小卡片保留。分类标题采用相同的 14px Semibold 标签样式：主因素红色 #E21818、次因素橙色 #C65313、参考因素蓝色 #315EEF、排除因素深灰 #344054。
- 主次因素内部最新结构：「分类与因素名称 → 判断与依据 → 影响指标」。两项指标各用一张独立灰底卡片，沿用核心指标的形式：#F3F5F7 灰底、10px 圆角、12px 内边距，名称在上、数值在下，间距 12px。两卡间距 8px、各占一半可用宽度，整组铺满父容器；取消此前 450px 宽度上限，外部不再叠加填色容器。数字统一 24px Oswald SemiBold，字段名称为 13px #344054，数字与单位保持基线对齐，行高 100%。占比保留对应重点色，估算电量为 #344054；数值前删除「约」。判断标题与正文放在同一说明组内、间距固定 2px，说明正文 12px / 18px。
- 主次因素通过 CSS subgrid 共享标题、说明组、指标三个内容行，两卡相应内容起点对齐，说明组内部按文字自然换行，指标固定在卡片内容底部；不支持 subgrid 时由纵向弹性布局保持指标底部对齐。
- 默认主因素为「竞品情况」：61.8%、522.2 度；次因素为「价格情况」：38.2%、322.8 度。数值保持原型不变，仅删除数值前的「约」，字段名称仍为「影响充电量估算」。判断摘要和说明均保留原型内容。
- 参考因素为「设备情况 / 天气情况 / 活动分析」，只展示名称和原型结论。三项名称统一为 #111827 深色、14px Semibold；每项使用 #F3F5F7 灰底、无描边、8px 圆角和 12px 内边距，同排等高、间距 8px。外层「参考因素」分组仍保留统一的白底外框和蓝色分类标识。已删除设备情况额外的分类规则说明，不将 PRD 分类规则作为本次事件结论展示。排除因素仅显示「用户评论 / 社会事件 / 节假日」名称。
- PRD 4.1 约束：主因素为有效强因素中影响占比第一，仅 1 个；次因素为第二，最多 1 个；参考因素和排除因素可多项。本次交付为静态 UI 示例，正式接入时由业务数据提供分类，不根据名称或示例数值在前端推断。缺少次因素时可移除对应卡片，主因素自动占满一行；参考列表按数量自适应换行。
- PRD 4.1.1 提供更完整的文案模板；本原型缺少价格调整次数、均价变化幅度、故障时长等实值，因此保留原型已有的完整说明，没有把 PRD 中其他示例的数字混入本事件。
- 建议动作：正文使用原生 ul/li 无序列表，分别展示通知建议和调价建议。调价区间 -0.03 至 -0.01 元/度、生效时间 2026-08-19 00:00 至 2026-08-26 00:00、按时段调整均保留在第二条纯文本中，不再拆分成图标、子标题、字段或强调数字。列表统一使用正文 PingFang SC、14px Regular / 22px 行高、#344054，条目间距 8px，自然换行。
- 「推荐进入调价决策」仍为紧邻模块标题右侧的静态标签、间距 8px；模块完整展示，不发送通知或执行策略。
- 实际分析容器宽度至少 600px 时主次因素两列，否则单列；至少 720px 时参考因素根据空间自动分列，否则单列。所有新增图标复用已有 SVG，无新增外部资源、字体或脚本依赖。
- 改动前备份：`.history/2026-09-12-before-analysis-factors-160413/`，包含 HTML、布局样式与 README。新增文件为 `css/factor-analysis.css`。
- 浏览器核验：1920px 和 1280px 下主次因素两列、参考因素三列，1024px 下均为单列；模块间距均为 8px。已确认建议动作与分析因素同级且排在最后，无文字溢出、资源缺失、重复 ID 或控制台错误；原有两组分析数据未改动。
- 最新层级修订核验：1920px 下主次因素的判断与指标左右排，分析因素模块高约 392px；1024px 下恢复上下排，无文字溢出或资源错误。四类分组的背景、描边、标题颜色及字号均一致，结论文字和建议动作保持原文。修订前备份：`.history/2026-09-12-before-factor-hierarchy-161802/`。
- 重点色修订核验：保留上述结构、字号与同级关系，只调整分类标识、判断关键词及指标色彩；当前 1280px 预览中模块高度保持 518px，无内容溢出。修改前备份：`.history/2026-09-12-before-factor-accents-162339/`。
- 最新排列修订核验：1920px 下两张主次因素卡片内容高度均为 210px，指标区和判断摘要的起始位置分别对齐；1280px 下两项指标列宽各 160.5px，参考因素三卡均高 140px。未改动原型结论或指标数值。修改前备份：`.history/2026-09-12-before-factor-balance-163115/`。
- 阅读顺序修订核验：1280px 下主次因素两卡均高 225px，判断、指标及依据的起始位置分别对齐，参考因素三卡均高 166px；1920px 下主次因素均高 203px，参考因素三卡均高 124px；1024px 下主次与参考因素恢复单列，指标与文字无溢出。折叠及重新展开正常，主次因素数值、原型判断和完整依据均保留。修改前备份：`.history/2026-09-12-before-factor-reading-order-165422/`。
- 外框与指标顺序最新修订核验：1280px 下四类因素均为 1px 外框、12px 内边距，同列分类标题左对齐；四项指标数字统一 24px，均位于名称上方，两项等宽、间距 16px，数字至名称间距 8px。1024px 下单列布局无页面或指标溢出。修改前备份：`.history/2026-09-12-before-factor-metric-order-170039/`。
- 灰底容器修订核验：1280px 与 1920px 下两组指标容器均宽 300px、左侧与分类标题对齐，位于判断前方；参考因素三项均为 #F3F5F7 灰底、0px 描边，设备卡额外分类说明已移除。1024px 下无页面或指标溢出。修改前备份：`.history/2026-09-12-before-factor-gray-panels-170552/`。
- 独立指标卡修订核验：1280px 下每项灰底卡宽 146px、两卡间距 8px、外层透明，字段名称在数值上方，两个估算值均无「约」前缀；主次因素判断标题与正文间距均为 4px，无页面溢出。修改前备份：`.history/2026-09-12-before-separate-factor-metrics-170913/`。

## 分析因素说明文字修订

主因素、次因素、设备情况、天气情况、活动分析五处说明统一 12px Regular / 18px 行高，距各自上方标题 2px；标题与指标字号保持原样。

## 分析因素卡片布局（2026-09-14 最新）

- 主因素、次因素、每一项参考因素及排除因素在同一网格中按顺序排列，宽屏每行 4 张，不再保留「参考因素」总容器和内部灰底嵌套。
- 每张卡片使用纯白底、1px 轻描边、12px 圆角与内边距；恢复原有头部顺序：分类标签在左，因素名称紧跟右侧，间距 4px。参考因素只展示名称、分类和原有说明，不增加影响指标。
- 同行卡片等高，主次因素继续先说明原因，底部展示两项按内容宽度排列的灰底短标签；指标名称与数值同行，实际高度约 29px，数字 16px；参考因素说明保持 12px、与标题间距 4px。
- 卡片间距 8px。内容容器宽度达到 1280px 时四列，880 至 1279px 时三列，600 至 879px 时两列，更窄时单列，避免指标和标题被挤压。排除因素作为普通卡片参与排列，内部因素标签使用 #F3F5F7 灰色填充、无描边。
- 修改前备份：`.history/2026-09-14-factors-three-columns-094352/`。以上布局取代本文此前的「主次并排、参考因素单独一组」描述。

头部与排除因素卡片修订前备份：`.history/2026-09-14-factor-label-first-094959/`。

紧凑指标与四列布局修订前备份：`.history/2026-09-14-factor-compact-metrics-095506/`。短标签左对齐、不拉满父容器，空间不足时自然换行。

标题间距与字重修订：主次因素头部与下方内容间距 4px；二级说明标题使用 400 正常字重；参考因素头部下方间距及卡片底部内边距均为 4px。备份：`.history/2026-09-14-factor-spacing-weight-095948/`。


## 异常定位布局切换（2026-09-14 最新）

- 默认用户结构分析、充电入口分析、充电时段分析一行三列；综合判断保留在下方通栏。内容容器不足 760px 时按单列适配。
- 默认时段表格最高 132px（表头和约两行半数据），超出后内部竖向滚动，底部白色渐变在滚到底时隐藏。
- 只有默认表格实际溢出时，标题右侧才出现「展开」；展开后左列显示用户结构与充电入口上下两张卡片，右列时段卡片与左列合计高度一致，仍超出的内容继续内部滚动。
- 展开状态保留「收起」，点击恢复三列；按钮支持键盘，字体加载与窗口宽度变化后自动重算。空列表、全天统一价及内容未超高时不出现无效展开入口。
- 本次仅调整异常定位布局与交互，保留各 Demo 原有业务数据、定量/定性表格和其他模块。以上替代此前固定两列的布局说明。
- 修改前备份：`.history/2026-09-14-anomaly-expand-layout-101015/`。
- 已核验：1280px 下默认三列且表格无横向溢出；1920px 展开双列等高；1024px 自动单列；八时段滚动与末尾遮罩、三时段定性表格、全天统一价、两时段与空数据的按钮显隐。脚本语法检查通过，业务 JSON 和其他模块代码保持不变。

## 生命周期分组选中

并排展示的阶段在右侧导航共用一整块浅蓝选中背景，编号、名称、时间及各自锚点保持独立；点击其中任一项或滚动到该组时同步高亮。窄窗口改为上下排列时恢复分别高亮，隐藏阶段不参与分组。
